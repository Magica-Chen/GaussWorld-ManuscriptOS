# Journal Packs

Built-in packs:

- `elsevier`
- `springer_nature`
- `custom`

`custom` is the escape hatch for arbitrary user templates. `journal/JASA-template/` should be handled as the first worked custom-template example rather than a hardcoded journal family.
